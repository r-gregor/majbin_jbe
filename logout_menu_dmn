#! /usr/bin/env bash

choice=$(echo -e "logout\nreboot\nshutdown" | dmenu -l 3)

case $choice in
	logout)
		sudo loginctl terminate-user ${USER}
		;;
	
	reboot)
		sudo reboot
		;;
	
	shutdown)
		sudo systemctl poweroff
		;;

	*)
		exit
	;;
esac


